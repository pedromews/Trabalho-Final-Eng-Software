{
  "name": "my-app",
  "version": "1.0.0",
  "scripts": {
    "start": "concurrently \"npm run frontend\" \"npm run backend\"",
    "backend": "cd backend && npm start",
    "frontend": "cd frontend && http-server --default /index/"
  },
  "dependencies": {
    "concurrently": "^6.2.1"
  }
}
